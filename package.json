{
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.2"
  },
  "devDependencies": {
    "@openzeppelin/test-helpers": "^0.5.13"
  },
  "scripts": {
    "deploy": "cd client;yarn build;cd ..;firebase deploy",
    "deploy-functions": "firebase deploy --only functions"
  }
}
